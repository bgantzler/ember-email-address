{{page-title 'Index'}}

<DocsHero />

<div style='width: 40rem; margin: 0 auto; padding: 0 1.5rem'>
  <DocsDemo as |demo|>
    <demo.example name='my-demo.hbs'>
      <EmberEmailAddresses@EmailAddresses
        @onChange={{this.onChangeSelected}}
        @selectedEmails={{this.selectedEmails}}
        @onCreate={{this.onCreate}}
        @onSearch={{this.onSearch}}
      />
    </demo.example>
  </DocsDemo>
</div>

{{outlet}}