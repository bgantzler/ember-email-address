<div data-test-email-select ...attributes>
  {{#if @label}}
    <label>
      {{@label}}
    </label>
  {{/if}}
  {{log 'selectedEmails' @selectedEmails}}
  <PowerSelectMultipleWithCreate
    @triggerClass={{this.triggerClass}}
    @options={{@selectedEmails}}
    @selected={{@selectedEmails}}
    @onChange={{@onChange}}
    @onCreate={{@onCreate}}
    @renderInPlace={{true}}
    as |option|
  >
    {{#if (has-block)}}
      {{yeild option}}
    {{else}}
      {{option.email}}
    {{/if}}
  </PowerSelectMultipleWithCreate>
</div>